# 矿业新闻日报 · 系统性全面检查报告

- **检查时间**：2026-09-08 08:26–08:52（09:00 日常抓取之前）
- **检查方式**：只读诊断，未修改任何业务文件；两路并行代码审查 + 实跑校验脚本 + 线上比对
- **检查范围**：底层代码 / 产品逻辑 / 用户体验 / 潜在漏洞 / 功能实用性 / 界面简洁性 / 业务数据质量 / 运行性能 / 可维护可配置性 / 输出合规性
- **代码基线**：commit `9f5d566`，线上 build `20260908-0755`

## 严重度总览

| 级别 | 数量 | 含义 |
|---|---|---|
| **P0** | **3** | 线上故障 / 凭据泄露 / 功能从未生效 |
| **P1** | 12 | 明显影响正确性或体验，应尽快修 |
| **P2** | 11 | 优化项，可做可不做 |

---

## P0 · 必须立即处理

### P0-1 两个 API 凭据已泄露到公网

| 凭据 | 位置 | 暴露范围 |
|---|---|---|
| LME 行情接口 token | `fetch_lme.py:15` 硬编码 `58b2fa8f…` | 文件已 `git ls-files` 跟踪，**已推至 GitHub 公开仓库** |
| DeepSeek API Key | `index.html:3306` `QA_DS_KEY_B64`（XOR+Base64 混淆） | **线上页面可直接 curl 到**，访客 F12 即可还原 |

混淆不是加密。任何拿到的人都能还原并使用，产生费用或被封配额。

**处理**：轮换两个凭据 → 改从环境变量读取 → 清理 `fetch_lme.py` 的 git 历史（`git filter-repo` 或 BFG）→ 前端问答改为"未配置 Key 时降级到本地摘要"（已有 `localAiSummary` 兜底，不受影响）。

### P0-2 热榜与「AI 深度解析」两个区块从未显示过

`index.html:2002-2006`：

```js
document.querySelectorAll('.section').forEach(sec=>{
  const vis=sec.querySelectorAll('.news-item:not(.hidden)').length;
  sec.style.display=vis?'':'none';
});
```

隐藏逻辑只统计 `.news-item`，而热榜条目是 `li.hot-item`、AI 条目在 `.ai-list`（非 `.news-item`），故 `vis` 恒为 0 → 两个区块被置 `display:none`。

初始化 `applyFilter()`（:1957）跑在异步 `fetchHotNews()`/`fetchAiAnalyze()` **之前**，此时两区为空被隐藏；其后 `renderHotNews()`/`renderAiAnalyze()` 只写 `innerHTML`，全文件无代码重置这两区的 `display`。

**结果**：线上等于两个主打板块从不存在，且无任何报错。修复后才会第一次真正露面——届时需重新评估它们的实际观感。

---

## P1 · 应尽快修复

**代码正确性**
- `gen_today.py:69-90、287-293` 用 `html.replace()` 改日期与计数（如 `'id="tocAllCount">63<'`→`'55<'`），**匹配不上会静默跳过且无断言**，是计数与真实条目数不符的直接成因。
- `generate_20260901.py:647、844` 硬编码另一台机器的绝对路径 `C:/Users/windows/...`。

**安全**
- `fetch_news.py:54-56` 全局关闭 TLS 校验（`check_hostname=False`、`CERT_NONE`）。白名单只校验域名不校验传输，MITM 可注入伪造标题/URL。
- 外链 `target="_blank"` 共 95 处，**仅 13 处带 `noopener`**，存在 `window.opener` 反向操控风险。
- `sw.js:107` 数据文件兜底返回 `index.html`——`news-data.js` 拉取失败时把 HTML 当 JS 执行，必然 SyntaxError。

**产品逻辑**
- **「今日」实为昨日且无提示**：h1 徽章、`digestStrip`、简报均取 09-07，而 build 已是 09-08。用户早上打开会误以为是当日。
- **计数口径矛盾**：`meta.page_total=81` vs `total=79`、`page_new_count=3` vs `new_today=1`；`refresh()` 又把 `todayCount` 改成"共 N 条"（N＝全页 `is-new` 数）。
- `injectRightsResultSummary()` 往今日区插的合成卡，与矿权专区是同一批公告，同内容两处出现。

**体验**
- 首屏被价格淹没：DOM 顺序 header→简报→要闻→priceStrip（16 张卡）→stats→今日新增，移动端真正新闻在第 5 屏开外。
- `.qa-fab`（z-index 100000，bottom 16px）压在 `.mobile-install-bar`（z-index 200，bottom 0）之上，遮住安装条。
- 矿权 CSV 只导出 8 条（`shownList=list.slice(0,8)`），而全库 28 条，用户误以为拿到全量。

**性能**
- `index.html` 364KB，内联 JS 188KB（51.7%）+ CSS 74KB（20.2%），首屏阻塞约 172KB。

**可维护性**
- 配置散落四处：源配置 `fetch_news.py:105`、白名单 `source_whitelist.py:51`、保留窗口 `export_news_json.py:306`、阈值 `backcheck.py:60`。**新增一个源要改 2 个文件且无一致性校验**。
- `export_news_json.py:17` 文档称 `RETAIN_DAYS=30`，`:306` 实为 **365**——30 天窗口未生效，`news-data.js` 会持续膨胀。

---

## 业务数据质量（实测）

| 指标 | 实测 | 评价 |
|---|---|---|
| 总条目 | 79 | — |
| **摘要为空** | **27 条（34%）** | ⚠️ 主要短板 |
| 标题完全重复 | 0 | ✅ |
| 未来日期 | 0 | ✅ |
| 白名单违规 | 0（84 URL 全合规） | ✅ |
| 事件日期分布 | 最新 09-05，report_date 09-07 | ⚠️ 09-06/09-07 无事件入库 |

- 34% 条目无摘要，直接影响搜索命中与问答质量。建议在 09:00 流程中加一道"摘要为空则补写"的强制环节。
- 事件日期断档（最新 09-05 vs 报告日 09-07）需确认是采集缺口还是周末效应。

---

## 健康项（做得好的部分）

- **异常处理扎实**：`fetch_news.py` 17 处 `except` 全为具名类型，主干**零裸 except、零静默 pass**。
- **编码处理规范**：`utf-8→gbk→gb18030` 三级回退 + `errors='ignore'`。
- **XSS 总体可控**：`esc()`/`qaEsc()`/`qaSafeUrl()` 齐备，`news-data.js` 外部加载使标题含 `</script>` 也不会逃逸。
- **发布护栏有效**：`preflight_check.py` 6 类冒烟（marker 唯一性、div 收支平衡、关键函数存在性）实测全绿。
- **境外条目规范**：保留英文原题执行到位，未发现缺失。
- **爬虫健壮性**：`http_get` 带 `timeout=20` + `retries=2`，14 源配置化。

---

## 建议修复顺序

**第 1 批（安全，优先于一切）**
1. 轮换 LME token 与 DeepSeek Key，改走环境变量
2. 清理 `fetch_lme.py` 的 git 历史
3. 外链补齐 `rel="noopener noreferrer"`

**第 2 批（功能复活 + 正确性）**
4. 修 `applyFilter` 隐藏逻辑，让热榜/AI 区块显示（修后需实际验收观感）
5. `gen_today.py` 的字符串 replace 加断言，失败即中止
6. 统一计数口径，标题「今日」改为带日期的明确表述
7. 矿权 CSV 导出全量而非 8 条

**第 3 批（体验与性能）**
8. 价格区默认折叠 / 横向滚动，让新闻提前到首屏
9. 修复 qa-fab 与安装条遮挡
10. 搜索框防抖、scroll 节流
11. 拆出 `app.js` 并 `defer`

**第 4 批（可维护性，可择机）**
12. `RETAIN_DAYS` 与文档对齐（30 或 365 需先决策）
13. 配置集中化 + 加一致性校验脚本
14. 删除 13 个已废弃的一次性脚本（见附录）
15. 补摘要为空的条目

## 附录：可安全删除的废弃脚本

`generate_20260830.py`、`generate_20260831.py`、`generate_20260901.py`、`generate_20260904.py`、`generate_20260905.py`、`generate_20260906.py`、`generate_20260907.py`、`generate_daily.py`、`update_analysis_20260905.py`、`update_analysis_20260906.py`、`update_analysis_20260907.py`、`patch_20260906_cats.py`、`patch2_20260906_intl.py`、`add_rights_20260907.py`

（均已被 `gen_today.py` 取代，无调用方。删除前建议先打 tag 留档。）
