# 矿业资讯速览 · UX 与功能优化清单（#6 / #7）

> 整理时间：2026-09-24 ｜ 基准 build `20260924-1700`（v10 已上线）
> 范围：用户 09-23 七项诉求之 **⑥ 整体 UX/交互再优化**、**⑦ 其他应优化处**。
> ①②③④ 已在 v10 落地；⑤ 暂未收录抓源由并发会话（Task #57）推进，本清单不重复。

## 0. 边界（先读，避免重复踩坑）
- 红线/废弃项（REFERENCE.md §6 / §9）：**不**复活 阅读模式、旧矿权表格视图 `#rightsTable`、渐变/发光/脉冲图标（`qaOrbPulse` 类）；简报 `briefSub` 固定「按分类摘要」、**不**出「必看 N 条 / 今日收录 N 条」；头部日期唯一来源 `.date-badge`；价格 **红涨绿跌**、月榜 N 由数据实算（禁写死「近 30 日」）。
- 已否决方向：**热门新闻榜**（样本极小/口径打架）、**简报要点层** `.brief-hl`（与今日要闻重复）。
- 事件·数据日历 / 快讯时间轴：可做**区块**（非第三视图），但须避开口径冲突（见 §三）。

## 一、#6 用户体验 / 交互优化

### A. 导航与寻路
| # | 现状问题 | 建议方案 | 优先级 | 红线/风险 |
|---|---|---|---|---|
| A1 | 长页面（今日/往期）无全局「回到顶部」；现仅收藏/历史聚合 TOC 内有 `toc-back-top` | 加全局浮动「↑ 顶部」按钮（滚动超一屏出现，纯 opacity 过渡，无发光） | P1 | 不得用渐变/发光球；落 `#topBar` 前缀区或 app.js 运行时 |
| A2 | 视图切换后无滚动位置记忆（收藏/历史已处理，今日↔往期未） | 视图切换后把首条命中项滚入视口并高亮锚点 | P2 | 配合 scroll-spy，勿破坏 `#companySection` 默认隐藏 |
| A3 | 移动端进「往期/收藏/历史」依赖「我的」面板，入口层级深 | 「我的」面板内把 往期/收藏/浏览记录 做成一级可点项 + 各带条数徽标 | P1 | 复用既有 `.mine-*` 句柄，勿新增口径 |
| A4 | 无 URL 锚点直达（分享/收藏某条、跳区块靠手动滚） | 引入 `#/section` 与 `#/news-<id>` 路由（`history.pushState`，不刷新）；刷新后定位 | P1 | 纯前端；勿影响生成脚本重建（锚点只运行时解析） |

### B. 搜索与筛选
| # | 现状问题 | 建议方案 | 优先级 | 红线/风险 |
|---|---|---|---|---|
| B1 | 移动端首页检索条 `#newsFilterBar` 无入口（`md-search-btn` 死代码，§11.3①） | 修复二选一：① 桌面隐藏规则改 `@media(min-width:769px)` 恢复图标；② 检索统一收进 `#qaFloat`（面板已有检索+AI），删死代码 | P0 | 须同步 `test_mobile_ux_batch.js` 断言 |
| B2 | 搜索仅限新闻流（分类+关键词）；公司/矿权不能一起搜 | 在 `#qaFloat` 内做「跨区检索」：新闻+公司名+矿权，结果分组展示 | P2 | 公司搜索已支持摘要匹配（v7）；矿权需接 `rightsType` |
| B3 | 关键词标签可点筛选，但不记忆（刷新即丢） | **关键词订阅（本地版）**：「我的」存一组关注词，启动即自动高亮/置顶命中条；无后端不真推送 | P1 | 纯 localStorage；不得承诺服务端推送 |
| B4 | 往期按日组折叠，无「跳到某天」 | 往期区加日期下拉/月份锚点快速跳转指定日组 | P2 | 不得与 `.digest-dtag` 日期口径冲突 |

### C. 阅读与信息密度
| # | 现状问题 | 建议方案 | 优先级 | 红线/风险 |
|---|---|---|---|---|
| C1 | v10 全局放大字号后，需复核卡片行高/间距是否拥挤（价格卡 5 轨、公司单栏） | 真实 Chrome 探针复核桌面/移动三档断点行高与卡片间距；微调 `--fs-*` 与 padding | P1 | 改动在 `<head>` 前缀区（抗重建）；同步 mobile 断言 |
| C2 | 新闻卡目前纯文字，扫读效率低 | 可选：给有图源的新闻加 16:9 缩略图，无图源走占位图标；**需先确认数据源能否稳定给图** | P2 | 不得引入外链追踪图；图片懒加载 |
| C3 | 长摘要截断规则未统一（简报≤80字精炼；新闻卡摘要长度？） | 新闻卡摘要统一「句末截断 + 内联展开全文」，避免半句 | P2 | 勿触碰简报 `BRIEF_DIGEST` 契约（§16） |
| C4 | 浏览记录已有，但已读条目未视觉弱化 | 历史视图内已读条加 `opacity` 弱化 + 「已读」标记；首页可选「隐藏已读」 | P2 | 仅视觉，不改数据 |

### D. 交互动效与手感
| # | 现状问题 | 建议方案 | 优先级 | 红线/风险 |
|---|---|---|---|---|
| D1 | 视图切换为瞬时显隐，长内容跳动 | 区块切换加 `opacity`/`transform` 150ms 轻过渡（尊重 `prefers-reduced-motion`） | P1 | **禁止**渐变/发光/脉冲；只用 opacity |
| D2 | 移动端小触控点（<44px）可能残留（字体放大后好转，需复核） | 全局审计触控目标 ≥44×44px（探针量 `getBoundingClientRect`） | P1 | 同步 `test_mobile_ux_batch.js` |
| D3 | 收藏/历史「清空」按钮无二次确认，误触即丢 | 清空前 `confirm()` 二次确认（桌面+移动一致） | P0 | 红线：`.mine-clear`/`.toc-clear` 两清空句柄必须存在 |
| D4 | 纯静态 PWA，无「最后更新时间 / 手动刷新」提示 | 顶部细条或安装指引区显示「数据更新于 HH:MM」+ 手动刷新（清 SW 缓存重载） | P1 | 与既有健康 banner（`__mdBootGrace`）共存，勿抢诊断位 |
| D5 | 无滚动进度指示（长页不知看到哪） | 顶部细进度条（1–2px，`currentColor` 描边，非发光） | P2 | 纯 CSS/JS，无发光 |

### E. 移动端专项（含 §11.3 待拍板三项）
| # | 现状问题 | 建议方案 | 优先级 | 红线/风险 |
|---|---|---|---|---|
| E1 | §11.3① 首页检索条无入口（死代码） | = B1，P0 合并处理 | P0 | 见 B1 |
| E2 | §11.3② ≤360px 品牌行日期被截（可用 99.5/59.5px） | 窄屏（≤360px）日期改短格式「09-11 周五」 | P1 | 改 `.md-date` 媒体查询；同步 mobile 断言 |
| E3 | §11.3③ 「AI 搜」常驻品牌色，稀释激活指示 | 激活态用「品牌色 + 圆角底」区分，非激活中性灰（复用 §35.2 底栏激活样式） | P2 | 须同步 `test_mobile_ux_batch.js:77` 断言 |
| E4 | 移动端底栏 4 项 + AI搜浮动，往期/收藏/历史需进「我的」 | 见 A3，把往期提到更浅层级 | P1 | — |

## 二、#7 其他应优化处
| # | 方向 | 建议方案 | 优先级 | 红线/风险 |
|---|---|---|---|---|
| F1 | 分享卡片（P1 推荐） | 选中新闻/简报 → 生成可下载图片卡片（canvas/SVG → PNG），纯前端、零口径风险 | P1 | 图标单色描边；不依赖后端 |
| F2 | 独立 URL + schema.org（P1 推荐） | 每区块/新闻带 `#/id` 深链（=A4），并注入 `JSON-LD` 结构化数据提升可检索性 | P1 | 与 A4 合并；JSON-LD 不影响渲染 |
| F3 | 周报（P1 推荐） | 纯前端聚合近 7 日高价值条目生成「周报」视图/卡片，可分享 | P1 | 复用热榜打分，不虚构指标 |
| F4 | 性能 | `app.js` 体积/执行审计；首屏骨架占位减少闪动；图片懒加载 | P2 | 不得破坏 `__mdBootGrace` 健康判定 |
| F5 | 无障碍 | `:focus-visible` 轮廓、skip-link、健康 banner `aria-live="polite"`、`prefers-reduced-motion` 全局尊重 | P1 | 配合 a11y landmark（已注入 banner/main） |
| F6 | 数据广度（≠⑤） | 公司名单扩围、矿权源增量；**⑤ 由 Task #57 推进，不重复** | P2 | 走 fetch_company.py 链路 |
| F7 | 事件·数据日历 / 快讯时间轴（可选区块） | 仅做**区块**（非第三视图）：日历从新闻派生可解析日期事件；时间轴仅覆盖今日、可折叠 | P2 | **口径冲突红线**：不得与 `#expoMini` 会展迷你卡 / 今日要闻重复；详见 §三 |

## 三、明确不建议做（已否决 / 高风险）
- ❌ 阅读模式（整套已删且 test 锁死）
- ❌ 旧矿权表格视图 `#rightsTable`（口径以自动化红线为准）
- ❌ 渐变/发光/脉冲图标（`qaOrbPulse` 类发光球）
- ❌ 简报「必看 N 条」「今日收录 N 条」（口径打架）
- ❌ 热门新闻榜（样本极小 / 按条数=已否决口径 / 右栏热榜已占位）
- ❌ 简报要点层 `.brief-hl`（与今日要闻重复）
- ❌ 把「事件日历 / 快讯时间轴」做成**第三视图**（与今日新增/今日要闻口径冲突）—— 只可做轻量区块
- ❌ Web Push 真推送（纯静态无后端、需 VAPID+推送服务，架构不符 → 暂缓）

## 四、建议实施顺序
- **P0（先修，低风险硬伤）**：B1/E1（移动检索入口）、D3（清空二次确认）。
- **P1（本轮建议主力）**：A1 全局回顶、A3/A4 移动导航浅化+深链、B3 关键词订阅、C1 字号后间距复核、D1 轻过渡、D2 触控审计、E2 窄屏日期、D4 更新时间、F1/F2/F3 分享+深链+周报、F5 无障碍。
- **P2（后续迭代）**：B2 跨区检索、B4 日期跳、C2 缩略图、C3/C4 展开全文+已读弱化、D5 进度条、F4 性能、F6 数据、F7 日历/时间轴区块。

> 注：P0 经核查已于 2026-09-12 检索统一（`.md-search-btn` 移除、移动检索改由顶部搜索条 + 底部「AI 搜」tab 经 `#qaFloat` 承载）与两段式清空确认（首点「确认清空？」+ 撤销 toast）中落地，**本次无需重做**。P1 已于 2026-09-24 批次执行（A1 全局回顶 / F5 焦点轮廓 / D1 视图淡入 / D4 更新时间+刷新 / A4 深链 / B3 关键词订阅 / E2 窄屏短日期），详见 REFERENCE.md §42.20。仓库当前**无并发写入者**；每一项落地均须走 `preflight → 对应测试 → bump build/CACHE → deploy`，新增模块须补 REFERENCE.md §42。
