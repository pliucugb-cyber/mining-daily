# ===== Python =====
__pycache__/
*.pyc
*.pyo

# ===== 爬虫候选池（fetch_news.py 中间产物，未经人工审核，不入库不部署）=====
data/news_candidates_*.json

# ===== 漏稿回溯报告（backcheck.py，本地人工核实用，逐日累积不入仓库）=====
data/backcheck_*.md

# ===== 前端临时测试脚本（jsdom 冒烟，用完即弃）=====
_test_*.js

# ===== 含密钥，禁止提交（DeepSeek API key 在 qa_config.json 里）=====
qa_config.json

# ===== 部署/运行时产生的缓存（按日期、机器相关，非源码）=====
# 注：data/ai_analysis_*.json 与 data/hot_news.json 是「静态站点预生成数据」，
#     需随仓库发布给 GitHub Pages 使用，因此【不再忽略】，正常提交。


# ===== 本地临时/诊断目录（约 4.6M 杂项，冒烟测试在下面例外保留）=====
tmp/*
!tmp/smoke_ai.js

# ===== 备份 / 打包产物 =====
*.zip
*.bak
*.bak-*
index_old_*.html
index.html.bak-*

# ===== 页面拼装时的零碎草稿（_ 前缀）=====
_*.txt

# ===== 一次性产物 =====
test.txt
screenshots/
online.html

# ===== 系统 / 编辑器 =====
.DS_Store
Thumbs.db
.idea/
.vscode/
worker/.wrangler/



# ===== Cloudflare Workers 本地构建缓存 =====

# ===== 本地凭据（不入 git）=====
# lme_token.txt：东财 futsseapi 行情 token。2026-09-10 实测：该接口不带 token 也返回同样数据，
#   它不是账号凭据（无吊销概念），仅作运行参数保管；若将来接口强制校验，去东财期货行情页
#   F12 Network 里重新抓一个 token 写进本文件即可。
# DeepSeek key 走 Netlify 平台环境变量。曾于 09-05~09-08 内嵌在静态页泄露，
#   09-08 止血时已轮换，旧 key 实测 401 失效（勿再用历史里的旧 key）。
lme_token.txt
netlify_token.txt
netlify/.site.json
.netlify/

# ===== 自动化运行时状态文件（锁/状态/预检结果，均不应提交）=====
.automation.lock
.last_run_status.json
.preflight_status.json

# ===== 本地 node_modules（仅测试用 junction，指向托管 node workspace，不入库）=====
node_modules/

# ===== WorkBuddy 会话记忆（2026-09-10 补）=====
# 本仓库是公开的，而 .workbuddy/memory/ 里是智能体的会话备忘录（含排查过程、
# 历史事故、凭据处置记录等内部信息），与站点无关。此前未被忽略，其中
# .workbuddy/memory/automations/**/memory.md 已被跟踪，随时可能被某次 git add 带上公开提交。
.workbuddy/
