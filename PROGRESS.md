# PROGRESS.md — 矿业日报自动化 Token 优化

最后更新：2026-09-11（第二轮优化）
状态：本轮优化脚本已落地并**已推送远端**；三套自动化 prompt 已重写并生效（**A「禁读 >50KB 文件」+ C「token 结构化落入 .last_run_status.json」已完成 2026-09-11**，B / D 待办）。

---

## 一、已完成改动（已完成）

### 1. 新增 `runq.py` — 长输出脚本的安静执行包装器
- 用法：`PY runq.py <脚本> [参数]`（PY = `C:/Users/中铝矿业投并部/.workbuddy/binaries/python/versions/3.13.12/python.exe`）
- 行为：把被包装脚本的**完整 stdout/stderr 落盘**到 `tmp/runq_*.log`，只回传**≤1800 字的精炼 stdout**（开头执行摘要 + 末尾报错栈 + 退出码）。
- 约束：**不截断报错堆栈**（含 Traceback/Exception/Error 时完整保留）、**不改动任何磁盘数据**、**不改动抓取到的新闻/行情/矿权数据**。
- 实测：8322 字节原始输出 → 回传 ~1977 字，磁盘日志完整保留。
- 目的：从 shell 层保证 stdout 不爆量回灌 LLM 上下文，不依赖模型自律。

### 2. 新增 `token_usage.py` — 本次会话 token 用量统计
- 扫 `~/.workbuddy/projects/<当前项目>/*.jsonl`，按 `CODEBUDDY_SESSION_ID` 聚合 `input_tokens / output_tokens / total_tokens / cache_read_input_tokens / calls`。
- 已优化：定位到当前项目目录后即停止全量扫描（耗时 ~0.6s，原全量扫所有项目慢）。
- 输出示例：`本次token 输入2,492,xxx(缓存命中2,412,xxx) 输出17,xxx 调用27 合计2,5xx,xxx`。

### 3. `notify_status.py` 接入 token 上报
- `fail / warn / ok` 三类通知的 `msg` 自动追加本次 token 用量（见上格式）。
- 效果：**三个自动化收尾通知都自动带 token 数**，可直接在通知里逐日跟踪花费。

> 上述三个文件已提交并推送：`87cd0b1`（feat: token 用量上报 + runq）、`e066584`（perf: token_usage 提速）。
> ✅ 2026-09-11 22:37 核对：`git status -sb` 无 ahead 标记，`HEAD` 与 `origin/main` 均为 `5434a7e`，**两个 commit 已在远端**（此前文档所记「尚未 push」已作废）。

### 4. 三套自动化 prompt 全部重写（节流 + 安静 + 瘦身）
| 自动化 | ID | 改动 |
|---|---|---|
| 矿业日报 06:00 抓取生成 | `5cdcdfff-4524-4083-9453-9f6577c7c7d6` | 嵌入节流规则、改用 `runq.py`、40→60 轮上限、压缩叙述约 40–50% |
| 矿业日报 08:00 复核 | `21dba82b-993b-4889-8381-77d621fd1c73` | 同上；保留「独立复核+修复+同步线上」职责 |
| 月度健康巡检 | `9651db87-ec44-4be6-adb4-488c71b0590b` | 同上；保留「纯检查、不改 index.html、不 deploy、不 push」红线 |

统一的「Agent 执行节流规则（强制启用）」节包含：
1. 长输出脚本一律 `PY runq.py` 执行（完整日志落盘、≤1800 字回传、不截断报错、不改磁盘）；
2. 每累计 12 轮工具调用做摘要压缩（只留进度/结论/关键报错，磁盘不动）；
3. 最大 60 轮，撞线走 release 锁 + fail 告警（不卡死下游）；
4. 禁止连续两次执行完全相同命令；
5. 只留业务结论在上下文，完整日志交 runq 落盘；
6. 含 Traceback/Exception/Error 的返回禁止截断。

**所有内容红线在压缩后原样保留**：7 桶分类、23 域白名单、矿权单视图（无 #rightsTable）、LME 口径（价格唯一来源=lme_data.json、走势图末点=昨天正确）、前端自愈引信（`#mdBootWarn`/`window.mdHardReset()`/`app.js` 末行 `__mdAppEvaluated=true`/顶层 `setTimeout(fn,0)` 防 TDZ）、独占性互斥锁协议、低价值公告剔除。

---

## 二、关键发现（决定怎么省才有效）

本会话实测：输入 249 万、**缓存命中 241 万（≈97% 命中）**。
- 平台 **prompt cache 已生效**，真正烧钱的是「每轮新塞进来的唯一上下文增量」，不是总量。
- 所以 `runq` 截断 / 12 轮摘要压缩 / 节流规则正好打在点上：压住每轮新增噪声 → 持续命中缓存。
- 13.6M 那次爆量，主因疑为**首跑无缓存 + 死循环重试把唯一上下文反复堆高**（即「禁连续相同命令」要治的根因）。

---

## 三、下一步（用户已提出，本轮按下「停止」未执行）

- **A（零风险，建议做）**：在三套自动化节流规则里再加一条——「禁止用 Read 读 >50KB 文件（index.html 341KB / app.js 281KB），局部一律 Grep、运行信息一律 runq 落盘」。这是单文件最大回灌隐患。
- **B（治本但略动脚本）**：给 `fetch_news.py` / `validate_urls.py` 等脚本本身加 `--quiet`，只 print 一行摘要，比 runq 包装更彻底；需逐个改，风险稍高。
- **C（可观测性）**：把 token 数也写进 `.last_run_status.json`，便于跨日画趋势对比（目前只在通知里出现）。
- **D（待定模型）**：长流程换更便宜模型是否划算——见下方「待讨论」。

---

## 四、D：长流程到底哪个模型更便宜？（待讨论，未结论）

- 当前自动化未显式指定模型，走平台默认（命中 prompt cache 后边际成本已很低）。
- 需先确认平台是否支持「per-自动化指定模型」以及可选的模型清单与单价，才能给出可落地的结论。
- 初步判断：长流程的省钱主战场是 **降低每轮唯一上下文增量（A/B/runq/压缩）**，而非换模型——因为 97% 的输入已被缓存覆盖，换更便宜但上下文更短的模型对缓存命中率可能有副作用。建议先把 A/B/C 落定、用 PROGRESS 里 C 的跨日 token 趋势跑两周后再决定是否换模型。

---

## 五、待补办事项清单
- [x] push 本地 `87cd0b1` / `e066584` 两个 commit —— 已完成（2026-09-11 22:37 核对，`HEAD = origin/main = 5434a7e`）
- [x] 执行 A：三套自动化加「禁读 >50KB 文件」规则（2026-09-11 完成）
- [ ] 执行 B：核心脚本加 `--quiet`
- [x] 执行 C：token 数写入 .last_run_status.json（notify_status.py 新增 _token_dict，收尾状态文件写入结构化 token 字段；2026-09-11 完成）
- [ ] 讨论 D：确认平台模型可选性与单价后再决策
