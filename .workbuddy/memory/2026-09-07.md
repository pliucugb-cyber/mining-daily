# 2026-09-07

## 补生成矿业日报（自动化恢复任务）
- 09:00 任务卡死导致 index.html 脏写（今日新增 marker 重复 18 处，且已提交进 HEAD）。本轮回退用 `git checkout` 无效——**HEAD 本身就是脏提交**，要先手工删 18 处重复注释才能还原干净基线。
- `generate_20260907.py` 两处 bug 已修：① 重建边界应切在 `rightsSection`（原切 `installGuideSection` 会误删矿权专区容器 + 丢失「详细安装指引」marker）；② 生成后需删掉 canon `<!-- 今日新增 -->` 注释，否则 preflight 报 marker 重复（要求恰好 1 个）。
- 产出：今日 9 / 往期 37 / 合计 46；矿权仅入 `data/news_2026-09.json`（主列表 0 条）；价格两行保留；无市场脉搏条。已 push main + 部署 gh-pages。
- 校验：DOM 自检通过；中国网与 news.com.au 从沙箱 HTTP 000（机房 IP 被 WAF 拦，非死链），和讯/华安期货 200 但关键词误报，均非真问题。
- 复用提示：以后若再出现「今日新增 marker 重复」，先怀疑 HEAD 已是脏提交，不要只靠 git checkout 还原。
