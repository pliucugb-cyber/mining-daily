# 2026-09-06（周日）

## 矿业日报 10:00 复核 —— 补跑完整生成流程

- **发现**：09:00 那轮生成未跑成功。判据：index.html mtime 停在 09-06 02:03 的功能提交，
  `<title>` 与 morning_report.json 仍为 2026-09-05，mining_news.json 停在 09-05 20:56。
  （lme-data.js / price-history.js mtime 为今天 09:56，是行情脚本单独跑过，干扰判断。）
- **处置**：补跑完整流程。新增脚本 `generate_20260906.py`（页面重建，7 日往期窗口）与
  `update_analysis_20260906.py`（四个分析文件重算），均沿用 09-05 版写法。
- **结果**：今日 10 条 + 往期 39 条 = 49；各处计数与 mining_news.json meta 一致；
  链接校验 49/49 通过；LME 六卡与热榜运行时渲染出值；线上与本地 index.html 字节级一致。
- **提交**：commit 2cf2c29 → push main；deploy_pages.py 推 gh-pages（8d0adba）。
- 周日休市，价格卡沿用 09-04 收盘，与 09-05 完全相同，属正常。

### 复用经验
- 判定 09:00 是否跑成：看 index.html/morning_report.json/mining_news.json 的 mtime + `<title>` 日期；
  动手补跑前先用 PowerShell 确认没有生成进程在跑。
- 采集新闻：curl 列表页 → 抽 href+标题+日期 → 再 curl 每条原文页核对标题与数字，禁止凭记忆写。
  worldmr.net 是 GBK；ky.mnr.gov.cn 列表日期与原文发布时间可能差 1 天，以原文为准。
- 运行时渲染校验脚本路径必须写 `C:/Users/...` 全路径，`~/.workbuddy/...` 会让 node 解析成 `C:\c\Users\...`。

## 10:30 分类修正补充（本轮）

- **发现两处质量问题**：①「紫金矿业：高位进阶后的"韧性突围"」是中国企业报道，被误归入
  🌐 国际矿业动态，导致国际类仅 1 条、四类配比失衡；② 补入国际条目时选中的
  「政府干预关键矿产"前所未有"」，经 news-data.js 比对 first_seen(n)=2026-09-04，
  属 09-04 已收录旧条目，算重复收录。
- **处置**：新增 `patch_20260906_cats.py`（紫金归位行业动态 + 补 2 条国际）与
  `patch2_20260906_intl.py`（剔除已收录旧条目，换成 BMI 铁矿石展望）。增量打补丁，不重建整页。
- **结果**：矿权 5 · 找矿 2 · 行业 3 · 国际 2 = **12 条**；今日 12 + 往期 39 = 51；
  链接校验 51/51 通过（0 失效 0 告警）；commit 050d2fa → push main；deploy_pages 推 gh-pages（617b2f5）。
- 线上复验通过：标题 2026-09-06、is-new 12 条、BMI 条目上线、旧重复条目已下线、沪铜 108,780。

### 复用经验（本轮新增）
- **判"是否新条目"不能只看页面**：候选条目必须先在 news-data.js 里查 `n`（first_seen），
  已收录过的就是旧条目，哪怕今天才在页面上出现。
- `len(html_str)`（字符数）与 `ls` 的字节数差异可达 15%+（中文 UTF-8 三字节），
  不要拿两者对比误判"内容丢失"，改用 `git diff --stat` 确认。
- 增量补丁优于整页重建：重建会把今日区滚入往期、并在月度归档留下残留条目。
- 本日出现**两个自动化实例并发跑同一任务**（09:00 轮与 10:00 复核轮）。并发时会互相覆盖
  index.html，动手前应先看文件 mtime 是否在最近 1 分钟内变动，确认没有对手进程再改。
